<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->

    </style>

    <style name="NewDialog" parent="@android:style/Theme.Dialog">
      <!--  <item name="android:windowFrame">@null</item>  (no diff, not even w/ black bg -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item> <!-- false=fullscreen -->
      <!--  <item name="android:windowContentOverlay">@null</item>  (no diff)-->
      <!--  <item name="android:windowTitleStyle">@null</item> (no diff)-->
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
      <!--  <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item> (no diff) -->
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

</resources>